import React from 'react';
import { View } from 'react-native';
import Svg, { Circle, G, Path } from 'react-native-svg';

const HueColorCircle = () => {
  const colors = [
    '#ff0000', // red
    '#ffff00', // yellow
    '#00ff00', // green
    '#00ffff', // cyan
    '#0000ff', // blue
    '#ff00ff', // magenta
    '#ff0000', // red (again)
  ];

  return null;
};

export default HueColorCircle;
